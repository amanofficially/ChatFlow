// DEMO MODE: Typing simulation handled in chatStore.js
export function useTyping(conversationId) {
  const startTyping = () => {}
  const stopTyping = () => {}
  return { startTyping, stopTyping }
}
